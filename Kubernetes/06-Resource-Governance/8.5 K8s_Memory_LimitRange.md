# 8.5 : Memory-Only LimitRange

https://drive.google.com/file/d/10viXBvrdtGSne5rKc6jwRsoa30IcNgj5/view?usp=sharing

### 🔍 YAML Breakdown

```yaml
apiVersion: v1
kind: LimitRange
metadata:
  name: memory-limit-range
  namespace: learning
spec:
  limits:
  - default:              # ← Memory limit if not specified
      memory: 500Mi       # = 500 Mebibytes
    defaultRequest:       # ← Memory request if not specified
      memory: 250Mi       # = 250 Mebibytes
    type: Container

```

> 🔑 Key Insight:
> 
> 
> This LimitRange **only manages memory** — CPU is **not constrained** (Pods can use any CPU unless restricted elsewhere).
> 

> 💡 Why memory-only?
> 
> - **Memory is incompressible**: Exceeding limits → **OOMKilled** (no recovery)
> - **CPU is compressible**: Exceeding limits → **throttled** (keeps running)
> → Memory requires **stricter, more predictable limits**

---

### 📌 How Memory Units Work

| Unit | Meaning | Notes |
| --- | --- | --- |
| `250Mi` | 250 **Mebibytes** | = 250 × 1024² bytes (binary) |
| `250M` | 250 **Megabytes** | = 250 × 1000² bytes (decimal) |
| **Kubernetes uses `Mi`/`Gi`** | ✅ **Always prefer binary units** |  |

> ✅ Best Practice:
> 
> 
> Use **`Mi` (Mebibytes)** and **`Gi` (Gibibytes)** for clarity and consistency.
> 

---

### 🧪 k3s Lab: Test Memory-Only LimitRange

### 🔧 Step 1: Create Namespace & Apply LimitRange

```bash
# Create namespace
kubectl create namespace learning

# Apply LimitRange
kubectl apply -f namespace-memory-limitrange.yml

# Verify
kubectl describe limitrange memory-limit-range -n learning
# Type       Resource  Min  Max  Default Request  Default Limit
# Container  memory    -    -    250Mi             500Mi

```

### 🔧 Step 2: Deploy Pod with NO Memory Resources

```yaml
# pod-no-memory.yaml
apiVersion: v1
kind: Pod
metadata:
  name: no-memory-pod
  namespace: learning
spec:
  containers:
  - name: nginx
    image: nginx

```

```bash
kubectl apply -f pod-no-memory.yaml

# Check applied memory resources
kubectl get pod no-memory-pod -n learning -o jsonpath='{.spec.containers[0].resources}'
# ✅ Output:
# {"limits":{"memory":"500Mi"},"requests":{"memory":"250Mi"}}

```

> 🔍 Result:
> 
> 
> LimitRange **auto-applied memory defaults** → Pod has **250Mi request, 500Mi limit**
> 

### 🔧 Step 3: Trigger OOMKilled (Exceed Memory Limit)

> 💡 Deploy a Pod that tries to use 600Mi ( > 500Mi limit):
> 

```yaml
# pod-oom-test.yaml
apiVersion: v1
kind: Pod
metadata:
  name: oom-test
  namespace: learning
spec:
  containers:
  - name: stress
    image: lovelearnlinux/stress:latest
    resources:
      requests:
        memory: "300Mi"
      limits:
        memory: "500Mi"   # ← Hard ceiling
    command: ["stress"]
    args: ["--vm", "1", "--vm-bytes", "600M", "--vm-hang", "1"]

```

```bash
kubectl apply -f pod-oom-test.yaml

# Watch it get killed
kubectl get pods -n learning -w
# oom-test   0/1   OOMKilled   1     20s

# Verify reason
kubectl describe pod oom-test -n learning | grep -A 2 "Last State"
# Last State: Terminated
#   Reason: OOMKilled
#   Exit Code: 137

```

> 💥 Key Insight:
> 
> 
> Even though the Pod **defined its own limits**, the LimitRange **didn’t override them** — but the **500Mi limit caused OOMKilled** when exceeded.
> 

### 🔧 Step 4: Add `max` to Prevent Over-Requesting (Recommended)

> ⚠️ Your current LimitRange has no max → users can request 10Gi and exhaust node memory!
> 

```yaml
# Improved LimitRange (with max)
spec:
  limits:
  - default:
      memory: "500Mi"
    defaultRequest:
      memory: "250Mi"
    max:                # ← ADD THIS!
      memory: "1Gi"     # Max 1 GiB per container
    type: Container

```

### 🔧 Step 5: Clean Up

```bash
kubectl delete pod --all -n learning
kubectl delete limitrange memory-limit-range -n learning
kubectl delete namespace learning

```

---

### 💡 Real-World Memory LimitRange Strategies

| Workload Type | `defaultRequest` | `default` | `max` | Why |
| --- | --- | --- | --- | --- |
| **Web Server** | 128Mi | 256Mi | 512Mi | Handles traffic spikes |
| **Database** | 1Gi | 2Gi | 4Gi | Needs consistent memory |
| **Cache (Redis)** | 512Mi | 1Gi | 2Gi | Memory = performance |
| **Sandbox** | 64Mi | 128Mi | 256Mi | Prevents node OOM |

> ✅ Best Practices:
> 
> - **Always set `max`** in production
> - **`requests` ≈ average usage**, **`limits` = `requests` + buffer**
> - **Monitor memory usage** (`kubectl top pods`) to tune defaults
> - **Test OOM scenarios** in staging

---

### 🆚 Memory vs CPU LimitRange

| Aspect | **Memory LimitRange** | **CPU LimitRange** |
| --- | --- | --- |
| **Enforcement** | OOMKilled (crash) | Throttling (slowed) |
| **Default Strategy** | `requests` ≈ `limits` (no bursting) | `requests` < `limits` (bursting OK) |
| **Risk of No Max** | **Critical** (node instability) | High (node slowdown) |
| **Units** | `Mi`, `Gi` (binary) | `m`, cores (decimal/milli) |

> 💡 Golden Rule for Memory:
> 
> 
> **"Set limits you can afford to lose"** — because exceeding them **kills your app**.
> 

---

### ❓ Common Questions

**Q: What if I set `defaultRequest` > `default`?**

A: ❌ **Invalid!** Kubernetes rejects with:

`requests.memory cannot be greater than limits.memory`

**Q: Does memory request affect scheduling?**

A: ✅ **Yes!** Scheduler ensures node has **≥250Mi free memory** before placing Pod.

**Q: Can I use `M` instead of `Mi`?**

A: ✅ **Yes**, but **`Mi` is standard** (1 MiB = 1024², 1 MB = 1000²).

**Q: What’s the smallest memory request?**

A: **1Mi** — but **64Mi+ recommended** for real apps.

---

### ➡️ Summary

✅ **Memory-only LimitRange** = control memory defaults separately

✅ **`defaultRequest`** = guaranteed memory (for scheduling)

✅ **`default`** = max memory (for OOM protection)

⚠️ **Always add `max`** to prevent node instability

🔍 In k3s: Test with **stress Pod** → watch **OOMKilled**

➡️ Next: **Comprehensive min/default/max** (`limit-ranges-default-min-max.yaml`)